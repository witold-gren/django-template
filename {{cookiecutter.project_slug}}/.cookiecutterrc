# This file exists so you can easily regenerate your project.
#
# `cookiepatcher` is a convenient shim around `cookiecutter`
# for regenerating projects (it will generate a .cookiecutterrc
# automatically for any template). To use it:
#
#    pip install cookiepatcher
#    cookiepatcher gh:ionelmc/cookiecutter-pylibrary project-path
#
# See:
#    https://pypi.python.org/pypi/cookiecutter
#
# Alternatively, you can run:
#
#    cookiecutter --overwrite-if-exists --config-file={{cookiecutter.project_slug}}/.cookiecutterrc git@github.com:witold-gren/django-template.git

default_context:
{% for key, value in cookiecutter.items()|sort %}
    {{ "{0:32}".format(key + ":") }} {{ "{0!r}".format(value).strip("u") }}
{%- endfor %}
